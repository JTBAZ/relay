node_modules
dist
.relay-data
.relay-backups
# Manual JSON archives after DB cutover (keep README only in git)
relay-data-archive/*
!relay-data-archive/README.md
.env
*.pem
.cursor/

# Prisma — commit prisma/schema.prisma + prisma/migrations/; ignore local-only env next to schema
prisma/.env
prisma/.env.*
