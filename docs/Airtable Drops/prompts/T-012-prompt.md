# T-012 — P2 — Analytics foundation / Action Center cards (Workstream E)

## Goal

Move toward **analytics-action-center-spec.md**: at least **three actionable insight cards** and **insight job success ≥99%** (or current spec targets). Build the minimum foundation without compromising pipeline reliability work.

## Scope / non-goals

- **In scope:** Card surfaces, job reliability, data sources agreed in spec.
- **Non-goals:** Full BI platform; blocking P0/P1 reliability tasks.

## Validation

- `npm run test` / `npm run build` for `web/` and `src/` as touched.
- Document how each card is populated and verified.

## Handoff

After success, write **Delta Out** and follow **Runs** logging in `docs/database/AIRTABLE_AUTOPIPELINE.md`.
