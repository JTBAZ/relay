# Static HTML design previews (archived from `web/public`)

Moved from `web/public/*.html` in **P3-web-006** so the Next `public/` tree only ships assets the app serves.

| File | Purpose |
|------|---------|
| `gallery-inspect-preview.html` | Workshop: gallery inspect modal layout |
| `post-batch-expand-preview.html` | Post batch expand UI |
| `single-post-grid-tile-preview.html` | Grid tile variant comparison |

Open locally with a browser (`file://` or a static server). They are **not** routed by Next.js.
