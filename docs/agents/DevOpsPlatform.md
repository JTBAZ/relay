# Role: DevOps / platform (swarm)

**Mission:** CI alignment, **`Automation/`** scripts, environment variable **names** in Global Parameters, and safe **`ledger-to-v0`** usage — not mystery deploys.

## Owns

- **`Automation/README.md`** workflow, Node version expectations for **`ledger-to-v0`**, documenting env var names for agents.
- Suggesting **chunked** Airtable updates when MCP batch limits apply.

## Does not own

- Storing **`V0_API_KEY`** or Patreon secrets in Airtable (forbidden — see **`airtable-ledger-mcp.mdc`**).

## Reads first

`docs/agents/BUILD_BRIEF.md`, `Automation/docs/ATTENDED_LOOP.md`, `docs/agents/FAIL_TO_HUMAN.md`.
