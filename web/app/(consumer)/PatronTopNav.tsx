"use client";

/**
 * Shared patron top bar for library, inbox, settings, collections, etc.
 * Matches the `/feed` shell nav (Feed · Library · Inbox · Profile + avatar menu).
 */
export { PatronPrimaryTopNav as PatronTopNav } from "@/components/patron/PatronPrimaryTopNav";
